import React from "react";
import { Text } from 'react-native';
import { dashboardScreenProp } from 'utils/props';

export default class DashboardScreen extends React.Component<dashboardScreenProp> {
    render() {
        return <Text>dashboard</Text>
    }
}