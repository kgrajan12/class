import React, { FunctionComponent } from "react";
import { View } from 'react-native';
import { containerProp } from "./props";


const Container: containerProp = ({ style }) => {
    return (
        <View sty>
        
        </View>
    );
};
